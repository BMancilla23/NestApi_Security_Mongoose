// Definimos un objeto llamado `environments` que contiene las rutas a los archivos de configuración de entorno
export const environments = {
    // En el entorno de desarrollo (dev), el archivo de configuración se llama ".dev.env"
    dev: ".dev.env",
    // En el entorno de producción (prod), el archivo de configuración se llama ".prod.env"
    prod: ".prod.env"
};

